extends layout

block content
  h1= title

  if products_list.length
    ul
      each product in products_list
        li
          a(href=product.url) #{product.name}
  else
    p There are no categories.
